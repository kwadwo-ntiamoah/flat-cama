part of 'events.dart';

class EventUpcoming extends StatelessWidget {
  const EventUpcoming({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Upcoming Events"),
    );
  }
}
