part of 'events.dart';

class EventPast extends StatelessWidget {
  const EventPast({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Past Events"),
    );
  }
}
