part of 'events.dart';

class EventLive extends StatelessWidget {
  const EventLive({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Live Events"),
    );
  }
}
